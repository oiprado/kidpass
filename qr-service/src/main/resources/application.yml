management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans

spring:
  config:
    import: "optional:configserver:"
  application:
    name: qr-service
  data:
    mongodb:
      uri: mongodb://admin:admin@localhost:27017/kidpass-qr

server:
  port: 8090

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
