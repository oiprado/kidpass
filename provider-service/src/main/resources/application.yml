spring:
  config:
    import: "optional:configserver:"
  application:
    name: provider-service
  data:
    mongodb:
      uri: mongodb://admin:admin@localhost:27017/kidpass-provider

server:
  port: 8086

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
